/**
 * The maximum allowed dimension for a package.
 * @constant {number}
 * @default 0.2
 * @description The maximum allowed dimension is specified in meters.
 */
export const MAX_ALLOWED_DIMENSION = 0.2;
